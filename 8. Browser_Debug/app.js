// sertTimeout(function, milliseconds)
// setInterval(function, milliseconds)
// Event Loop 
// Concurrency


// ------------------------------- Browser Api ---------------------------
// location (assign, replace, reload, href)
// History (back, forward, go)
// cookies - Data store in small text file. store in key-value pair.
    // document.cookie

// Local Storage - store data in browser without any expiration data. Data is not deleted when the browser is closed. 
// Session Storage - Store data in browser for a session. Data is deleted when browser is closed. (key, value)

//-------------------------------------------------------------------------------------
// Different            ||  Cookies             ||  Local          || Session        ||
// ------------------------------------------------------------------------------------
// Expires              || Manually set         || Never           || On tab close   ||
// Storage location     || Browser and Server   || Browser only    || Browser only   ||
// set with requests    || Yes                  || No              || No             ||
// capacity             || 4kb                  || 10mb            || 5mb            ||
// Browsers             || HTM4/HTML5           || HTML5           || Html5          ||
//-------------------------------------------------------------------------------------

// Local storage - (getitem(), setitem(), removeitem(), clear())




// -------------------------------------------------------------------------------
// Primitive vs Non-primitive
// Null vs Undefine
// Truthy vs Falsy
// (==)(only check value) vs (===)(check value and type)
// Scope (Function scope and Block scope) and Hoisting (Use for var)
// Closure, Encapsulation and Private variable
// Callback function
// pass by reference (object, array) and pass by value (number, string, bool)