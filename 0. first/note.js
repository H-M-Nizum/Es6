// =========================== JS NOTE ============================
// 1. --------------Fundamental Javascript
    // a. let vs const
    // b. Condition (>, <, ===, !==, <=, >=, &&, ||)
    // c. array  ([])
    // d. loop
    // e. function()
    // f. object  ({}) :- 3 ways to access property by name. obj.name, obj['name'], obj[variable_name]

// 2. --------------ES6
    // a. Template String (``)
    // b. Arrow function(const sum = () => {})
    // c. spread operator (...)
    // d. Array Methods (Map, Filter, Find, ForEach)
        // i. map() creates a new array from calling a function for every array element.
        // ii. filter() method creates a new array filled with elements that pass a test provided by a function.
        // iii. find() method returns the value of the first element that passes a test.
        // iv. forEach() method calls a function for each element in an array.
    // e. Array and Object Destructuring :(const [a, b] = [10, 20]  or  const {x, y} = {x:'23', y:45})
    // f. Optional chaining (?)

// -----------------------
    // a. JSON (Javascript object notation)(const jsondata = JSON.stringify(obj)) (const obj = JSON.parse(jsondata))
    // b. fetch
    // c. forEach, for of, for in
    // d. Truthy vs Falsey
    // e. Ternary operator and Shortcut
    // f. Locanstorage And Session Storage(getitem(), setitem(), clear())  
    